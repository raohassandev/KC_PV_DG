# PV-DG Commissioning (Expo)

- **Run:** `npm install` then `npm run android` (device/emulator + USB debugging).
- **Why Expo managed:** faster delivery than bare RN; use **prebuild / dev client** when you need extra native knobs. Bare RN only if you must own every Gradle line from day one.
