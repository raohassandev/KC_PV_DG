# API Simulator

Stateful LAN-first local device-service simulator for development of the controller HTTP contract.

```bash
npm run sim
```

The simulator is useful for exercising policy and telemetry contracts before the ESP32 firmware implements every endpoint.
