# Dynamic Zero Export API Simulator

This is a local LAN-style API simulator for the PWA during development.

It is not the firmware and not a device runtime.
It serves the shared local API contract over HTTP so the PWA can stop depending only on fixtures.

